# Food List — Production Environment Variables
# Copy to .env at build time on your CI/CD or build server
# NEVER commit the actual .env with real values to git

# Required: Google Maps JavaScript API key
# Restrict this key to your production domain in Google Cloud Console
# to prevent unauthorized usage
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
